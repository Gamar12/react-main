import React from 'react';
import btn from './Button.module.css'

const Button = () => {
    return (
        <button className={btn.button_20}>
            Create new post
        </button>
    );
};

export default Button;